<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.book.mappers.BoardMapper">

    <insert id="save" parameterType="com.book.Entity.BoardDTO">
        INSERT INTO board (title, writer, content) 
        VALUES (#{title}, #{writer}, #{content})
    </insert>

    <select id="findAll" resultType="com.book.Entity.BoardDTO">
        SELECT id, title, writer, content, reg_date 
        FROM board 
        ORDER BY id DESC
    </select>

    <select id="findById" parameterType="Long" resultType="com.book.Entity.BoardDTO">
        SELECT id, title, writer, content, reg_date 
        FROM board 
        WHERE id = #{id}
    </select>

</mapper>
